[Rust Crate Docs Index]|root: ./target/doc/fastview/index.html
|IMPORTANT: Prefer retrieval-led reasoning over pre-training-led reasoning

## Testing instructions
- When testing extract_metadata, you must test with all examples found in ./example_img/
- After every code change, build and test that the build completes, always check full build output (no | head command)
- After verifying the build completes commit to git with the received prompt in the commit message